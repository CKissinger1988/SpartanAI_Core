import logging
import random import subprocess import threading import time import os import json  class AutoUpdateService:     """     Sovereign Real-Time Auto-Update Engine.     Ensures all Jarvis/SentinelAI projects are perfectly synchronized with Git.     MANDATE: Full Send Protocol - Zero Delta maintained.     """     def __init__(self, projects_root=None):         self.root = projects_root or os.path.dirname(os.path.dirname(os.path.dirname(__file__)))         self.is_running = True         self.interval = 300 # Poll every 5 minutes         self.status = {}      def _sync_repo(self, path):         """Performs atomic synchronization for a single repository."""         repo_name = os.path.basename(path)         try:             # Mandate: Reset to hard-master to ensure no divergence             # 1. Fetch latest state             subprocess.run(["git", "-C", path, "fetch", "--all"], capture_output=True, check=True)             # 2. Reset to remote branch             res = subprocess.run(["git", "-C", path, "reset", "--hard", "origin/main"], capture_output=True, text=True)             if res.returncode != 0:                 # Fallback for 'master' branch                 res = subprocess.run(["git", "-C", path, "reset", "--hard", "origin/master"], capture_output=True, text=True)                          if res.returncode == 0:                 self.status[repo_name] = "SYNCED"                 return True             else:                 self.status[repo_name] = f"ERROR: {res.stderr}"                 return False         except Exception as e: logging.exception(e);             self.status[repo_name] = f"CRITICAL_FAILURE: {e}"             return False      def get_projects(self):         """Discovers all git repositories within the root."""         projects = []         for item in os.listdir(self.root):             full_path = os.path.join(self.root, item)             if os.path.isdir(full_path) and os.path.exists(os.path.join(full_path, ".git")):                 projects.append(full_path)         return projects      def sync_all(self):         """Orchestrates global synchronization."""         self.log_event("[AUTO-UPDATE]: Initiating system-wide synchronization sequence...")         projects = self.get_projects()         for p in projects:             self._sync_repo(p)         print(f"[AUTO-UPDATE]: Sync complete. Nodes online: {json.dumps(self.status, indent=2)}")      def run(self):         """Background thread loop."""         while self.is_running:             self.sync_all()             time.sleep(self.interval)  if __name__ == "__main__":     service = AutoUpdateService()     service.sync_all()
